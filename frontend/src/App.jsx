import Dashboard from './pages/Dashboard';

/**
 * App
 *
 * Root component. Currently renders only the Dashboard page.
 * If routing is added later, this is where <BrowserRouter> would go.
 */
export default function App() {
  return <Dashboard />;
}
